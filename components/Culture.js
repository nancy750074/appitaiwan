import React from 'react';
import { View,ScrollView, Linking } from 'react-native';
import { Button, Card, Text, PricingCard, Tile } from 'react-native-elements';

import { CultureTabRouter } from '../Router';

// Make a component
const Culture = () => (

    <CultureTabRouter />
  );


export default Culture;